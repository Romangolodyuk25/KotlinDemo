package `DevoColibri(Tutor)`

open abstract class Интерфейсы { //абстрактный класс не позволяет создать эксземплыяь var 1 = BaseTemplate() НЕТ, НО МЫ МОЖЕМ УНАСЛЕДОВАТЬ
    fun a () {
        println("vivod")
    }

}