fun main(args:Array<String>) {
    val numbers = arrayOf(1, 3, 4, 11, 17, 8, 15, 12)
    var index = 0
    while(index<numbers.size) {
        println(numbers[index])
        index = index + 2
    }
}